<span id="participation-count-<%= event.id %>"
      data-controller="participation-count"
      data-participation-count-event-id-value="<%= event.id %>"
      class="text-xs text-muted">
  参加予定 <%= event.participations.count(&:planning?) %>人・
  参加済み <%= event.participations.count(&:attended?) %>人
</span>
